Database password: ZQ1ZkIENYa5N6MRI

1. Kattintáskor tedd bele az ID-t az URL-be:
javascriptprodDiv.addEventListener("click", () => {
  window.location.href = `/product.html?id=${p.id}`
})
2. A sablon oldalon (product.html) olvasd ki az URL-ből és kérd le az adatot:
javascriptconst params = new URLSearchParams(window.location.search)
const id = params.get("id")

const res = await fetch(`/api/items/${id}`)
const product = await res.json()
3. A backenden egy új endpoint:
javascriptapp.get('/api/items/:id', async (req, res) => {
  const { id } = req.params
  const data = await sql`SELECT * FROM products WHERE id = ${id}`
  res.json(data[0])
})